# =============================================================================
# Chatbleton - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# =============================================================================
# Required - OpenAI API
# =============================================================================
OPENAI_API_KEY=sk-your-api-key-here

# =============================================================================
# Optional - Model Configuration
# =============================================================================
# Main chat model (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Speech-to-text model (default: gpt-4o-mini-transcribe)
OPENAI_STT_MODEL=gpt-4o-mini-transcribe

# Text-to-speech model (default: gpt-4o-mini-tts)
OPENAI_TTS_MODEL=gpt-4o-mini-tts

# =============================================================================
# Optional - Vector Store (RAG)
# =============================================================================
# Directory for ChromaDB persistence
CHROMA_DIR=chroma_db

# Collection name in ChromaDB
COLLECTION_NAME=ableton

# Embedding model (multilingual support)
EMBED_MODEL=intfloat/multilingual-e5-base

# =============================================================================
# Optional - Server Configuration
# =============================================================================
# Server port (default: 8000)
PORT=8000

# Debug mode - enables verbose logging (default: false)
DEBUG=false

# Environment: development or production (default: production)
FLASK_ENV=production

# =============================================================================
# Optional - Security Settings
# =============================================================================
# Maximum question length in characters (default: 2000)
MAX_QUESTION_LENGTH=2000

# Maximum audio file size in MB (default: 10)
MAX_AUDIO_SIZE_MB=10

# Rate limit: requests per minute per IP (default: 30)
RATE_LIMIT_PER_MINUTE=30

# =============================================================================
# Optional - LangSmith Telemetry (for debugging/monitoring)
# =============================================================================
# Uncomment to enable LangSmith tracing
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=lsv2_your_key_here
# LANGCHAIN_PROJECT=chatbleton-prod
